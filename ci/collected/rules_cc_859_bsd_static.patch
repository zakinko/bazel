diff -u -r a/cc/private/toolchain/lib_cc_configure.bzl b/cc/private/toolchain/lib_cc_configure.bzl
--- a/cc/private/toolchain/lib_cc_configure.bzl
+++ b/cc/private/toolchain/lib_cc_configure.bzl
@@ -239,6 +239,11 @@
         return "freebsd"
     if os_name.find("openbsd") != -1:
         return "openbsd"
+    if os_name.find("netbsd") != -1:
+        return "netbsd"
+    if os_name.find("dragonfly") != -1:
+        # The JVM spells it "dragonflybsd"; uname and GOOS say "dragonfly".
+        return "dragonfly"
     if os_name.find("windows") != -1:
         if arch == "aarch64":
             return "arm64_windows"
diff -u -r a/cc/private/toolchain/cc_configure.bzl b/cc/private/toolchain/cc_configure.bzl
--- a/cc/private/toolchain/cc_configure.bzl
+++ b/cc/private/toolchain/cc_configure.bzl
@@ -76,7 +76,7 @@
         repository_ctx.template("BUILD", paths["@rules_cc//cc/private/toolchain:BUILD.empty.tpl"], {
             "%{cpu}": get_cpu_value(repository_ctx),
         })
-    elif cpu_value == "freebsd" or cpu_value == "openbsd":
+    elif cpu_value in ("freebsd", "openbsd", "netbsd", "dragonfly"):
         paths = resolve_labels(repository_ctx, [
             "@rules_cc//cc/private/toolchain:BUILD.static.bsd",
             "@rules_cc//cc/private/toolchain:bsd_cc_toolchain_config.bzl",
diff -u -r a/cc/private/toolchain/bsd_cc_toolchain_config.bzl b/cc/private/toolchain/bsd_cc_toolchain_config.bzl
--- a/cc/private/toolchain/bsd_cc_toolchain_config.bzl
+++ b/cc/private/toolchain/bsd_cc_toolchain_config.bzl
@@ -57,7 +57,12 @@
 
 def _impl(ctx):
     cpu = ctx.attr.cpu
-    is_bsd = cpu == "freebsd" or cpu == "openbsd"
+    is_bsd = cpu in ("freebsd", "openbsd", "netbsd", "dragonfly")
+
+    # FreeBSD and OpenBSD build with clang and libc++.  NetBSD and
+    # DragonFly ship gcc and libstdc++ in the base system, so the C++
+    # runtime, the compiler path and the header locations all differ.
+    is_gcc_bsd = cpu in ("netbsd", "dragonfly")
     compiler = "compiler"
     toolchain_identifier = "local_{}".format(cpu) if is_bsd else "stub_armeabi-v7a"
     host_system_name = "local" if is_bsd else "armeabi-v7a"
@@ -73,6 +78,11 @@
     )
 
     action_configs = [objcopy_embed_data_action] if is_bsd else []
+    extra_default_link_flags = []
+    if cpu == "openbsd":
+        extra_default_link_flags = ["-lc++abi", "-lpthread"]
+    if is_gcc_bsd:
+        extra_default_link_flags = ["-lpthread"]
 
     default_link_flags_feature = feature(
         name = "default_link_flags",
@@ -83,10 +93,11 @@
                 flag_groups = [
                     flag_group(
                         flags = [
-                            "-lc++",
+                            "-lstdc++" if is_gcc_bsd else "-lc++",
+                            "-lm",
                             "-Wl,-z,relro,-z,now",
                             "-no-canonical-prefixes",
-                        ],
+                        ] + extra_default_link_flags,
                     ),
                 ],
             ),
@@ -242,7 +253,13 @@
     else:
         features = [supports_dynamic_linker_feature, supports_pic_feature]
 
-    if (is_bsd):
+    if cpu == "netbsd":
+        # Measured on 11.0: echo | /usr/bin/c++ -E -Wp,-v -xc++ -
+        cxx_builtin_include_directories = ["/usr/include/g++", "/usr/include/gcc-12", "/usr/include"]
+    elif cpu == "dragonfly":
+        # Measured on 6.4.2, base gcc 8.3.
+        cxx_builtin_include_directories = ["/usr/include/c++/8.0", "/usr/libdata/gcc80", "/usr/include"]
+    elif (is_bsd):
         cxx_builtin_include_directories = ["/usr/lib/clang", "/usr/local/include", "/usr/include"]
     else:
         cxx_builtin_include_directories = []
@@ -253,7 +270,10 @@
             tool_path(name = "compat-ld", path = "/usr/bin/ld"),
             tool_path(name = "cpp", path = "/usr/bin/cpp"),
             tool_path(name = "dwp", path = "/usr/bin/dwp"),
-            tool_path(name = "gcc", path = "/usr/bin/clang"),
+            tool_path(
+                name = "gcc",
+                path = "/usr/bin/gcc" if is_gcc_bsd else "/usr/bin/clang",
+            ),
             tool_path(name = "gcov", path = "/usr/bin/gcov"),
             tool_path(name = "ld", path = "/usr/bin/ld"),
             tool_path(name = "nm", path = "/usr/bin/nm"),
diff -u -r a/cc/private/toolchain/BUILD.static.bsd b/cc/private/toolchain/BUILD.static.bsd
--- a/cc/private/toolchain/BUILD.static.bsd
+++ b/cc/private/toolchain/BUILD.static.bsd
@@ -55,9 +55,13 @@
         "armeabi-v7a|compiler": ":cc-compiler-armeabi-v7a",
         "freebsd|compiler": ":cc-compiler-freebsd",
         "openbsd|compiler": ":cc-compiler-openbsd",
+        "netbsd|compiler": ":cc-compiler-netbsd",
+        "dragonfly|compiler": ":cc-compiler-dragonfly",
         "armeabi-v7a": ":cc-compiler-armeabi-v7a",
         "freebsd": ":cc-compiler-freebsd",
         "openbsd": ":cc-compiler-openbsd",
+        "netbsd": ":cc-compiler-netbsd",
+        "dragonfly": ":cc-compiler-dragonfly",
     },
 )
 
@@ -96,6 +100,74 @@
 )
 
 cc_toolchain(
+    name = "cc-compiler-netbsd",
+    all_files = ":empty",
+    ar_files = ":empty",
+    as_files = ":empty",
+    compiler_files = ":empty",
+    dwp_files = ":empty",
+    linker_files = ":empty",
+    objcopy_files = ":empty",
+    strip_files = ":empty",
+    supports_param_files = 0,
+    toolchain_config = ":local_netbsd",
+    toolchain_identifier = "local_netbsd",
+)
+
+cc_toolchain_config(
+    name = "local_netbsd",
+    cpu = "netbsd",
+)
+
+toolchain(
+    name = "cc-toolchain-netbsd",
+    exec_compatible_with = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:netbsd",
+    ],
+    target_compatible_with = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:netbsd",
+    ],
+    toolchain = ":cc-compiler-netbsd",
+    toolchain_type = "@bazel_tools//tools/cpp:toolchain_type",
+)
+
+cc_toolchain(
+    name = "cc-compiler-dragonfly",
+    all_files = ":empty",
+    ar_files = ":empty",
+    as_files = ":empty",
+    compiler_files = ":empty",
+    dwp_files = ":empty",
+    linker_files = ":empty",
+    objcopy_files = ":empty",
+    strip_files = ":empty",
+    supports_param_files = 0,
+    toolchain_config = ":local_dragonfly",
+    toolchain_identifier = "local_dragonfly",
+)
+
+cc_toolchain_config(
+    name = "local_dragonfly",
+    cpu = "dragonfly",
+)
+
+toolchain(
+    name = "cc-toolchain-dragonfly",
+    exec_compatible_with = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:dragonfly",
+    ],
+    target_compatible_with = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:dragonfly",
+    ],
+    toolchain = ":cc-compiler-dragonfly",
+    toolchain_type = "@bazel_tools//tools/cpp:toolchain_type",
+)
+
+cc_toolchain(
     name = "cc-compiler-openbsd",
     all_files = ":empty",
     ar_files = ":empty",
